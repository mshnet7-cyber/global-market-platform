import { readFileSync } from "node:fs"; const s=readFileSync("app/visual-polish.css","utf8"); const checks=["Theme System v3","--gmp-bg","--gmp-gold","html[data-theme="+"\"light\"]","data-font-scale","prefers-reduced-motion","gmp-success","gmp-danger"]; for(const c of checks) if(!s.includes(c)) throw new Error("theme contract missing: "+c); console.log("theme-system-v3: "+checks.length+" checks passed");