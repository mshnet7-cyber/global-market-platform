import {readFileSync} from "node:fs"; const s=readFileSync("supabase/migrations/20260922080000_gmp_gold_reconciliation_v1.sql","utf8")+readFileSync("supabase/migrations/20260922081000_gmp_gold_reconciliation_opening_backfill_v1.sql","utf8"); const ok=(n,c)=>{if(!c)throw new Error(n)}; ok("view",s.includes("gmp_gold_reconciliation")); ok("inventory delta",s.includes("inventory_ledger_delta")); ok("weight delta",s.includes("inventory_movement_delta")); ok("value delta",s.includes("value_delta")); ok("reconciled flag",s.includes("reconciled")); ok("opening backfill",s.includes("opening_balance")&&s.includes("on conflict (organization_id,idempotency_key) do nothing"));
ok("indexes",s.includes("gmp_inventory_movements_reconciliation_idx")&&s.includes("gmp_gold_ledger_reconciliation_idx")); console.log("gold-reconciliation: 6 checks passed");