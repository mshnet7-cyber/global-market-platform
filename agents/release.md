# Release Agent
Release only when: latest commit is identified, CI passes, deployment matches the commit, runtime is reachable, critical journeys pass, security gates pass, and remaining blockers are explicitly classified.
